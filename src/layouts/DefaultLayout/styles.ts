import { styled } from '@/styles'

export const Container = styled('div', {
  height: 'inherit',
  maxWidth: '90rem',
  display: 'grid',
  gridTemplateColumns: '14.5rem 1fr',
  padding: '1.25rem',
  gap: '6rem',
  margin: '0 auto',
})
