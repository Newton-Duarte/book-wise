import { styled } from '@/styles'

export const Container = styled('div', {
  maxWidth: '6.25rem',
  display: 'flex',
  alignItems: 'center',
  gap: '0.25rem',

  svg: {
    color: '$purple100',
  },
})
